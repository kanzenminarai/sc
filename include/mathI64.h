#pragma once

#include "../include/types.h"

// Calculates factorial using "const i64 value" and returns a 64 bit long integer value
i64 factorialI64(const i64 value);

// Calculates power using "const i64 number" and "const i64 expoent" and returns a 64 bit long integer value
i64 powerI64(const i64 number, const i64 expoent);
